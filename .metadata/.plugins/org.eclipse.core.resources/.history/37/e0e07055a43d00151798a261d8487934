package task0.tests;

import static org.junit.Assert.*;

import java.util.Arrays;

import org.junit.BeforeClass;
import org.junit.Test;

import task0.solutions.BinaryHeap;

public class BinaryHeapTests {
	static int size;
	static int[] testArray;
	static int[] workingArray;
	static BinaryHeap heap;
	@BeforeClass
	public static void setUpBeforeClass() throws Exception {
		size = 2000;
		testArray = TestGenerator.generateTestArray(size);
		workingArray = testArray;
		Arrays.sort(testArray);
		heap = new BinaryHeap();
	}

	@Test
	public void test() {
		for (int i = 0; i < workingArray.length; i++) {
			heap.insertMin(workingArray[i]);
		}
		
		for (int i = 0; i < testArray.length; i++) {
			assertEquals(testArray[i], heap.getTop());
			heap.removeMin();
		}

	}

}
