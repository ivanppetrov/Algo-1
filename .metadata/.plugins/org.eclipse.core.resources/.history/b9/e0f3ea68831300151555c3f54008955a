package task1.tests;

import static org.junit.Assert.*;

import java.util.Random;

import org.junit.BeforeClass;
import org.junit.Test;

import task1.solutions.BinarySearch;

public class SearchTests {
	private static final int NUMBER_OF_ARRAYS = 10;
	private static final int ARRAY_BOUND = 10;
	private static final int MIN_BOUND = 10;
	@BeforeClass
	public static void setUpBeforeClass() throws Exception {
		
	}

	@Test
	public void test() {
		Random random = new Random();
		for (int i = 0; i < NUMBER_OF_ARRAYS; i++) {
			int[] array = TestGenerator.generateTestArray(random.nextInt(ARRAY_BOUND) + 1);
			int searchedElement = array[random.nextInt(array.length - 1)];
			System.out.println(TestGenerator.isContain(array, searchedElement));
			System.out.println(BinarySearch.binarySearch(array, searchedElement, 0, array.length - 1));
		}
	}

}
